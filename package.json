{
  "name": "polygon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " concurrently 'cd server && nodemon server.js' 'cd client && yarn start' "
  },
  "author": "kushal sharma",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
